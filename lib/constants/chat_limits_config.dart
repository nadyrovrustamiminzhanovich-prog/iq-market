class ChatLimitsConfig {
  /// Максимальная длина текста одного сообщения в чате. Раньше не было
  /// ограничено ни на клиенте, ни в Firestore rules — можно было отправить
  /// текст произвольной длины (спам-простыни, DoS по размеру документа).
  static const int maxTextLength = 2000;
}
